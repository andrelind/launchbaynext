{
  "name": "lbn-platform",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev:web": "cd apps/web && bun run dev",
    "web:build": "cd apps/web && bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "apps/web",
    "packages/*"
  ]
}
